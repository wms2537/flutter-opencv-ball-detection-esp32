name: opencv_flutter_ffi
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  image_picker: ^1.0.4
  camera: ^0.10.3+2
  image: ^4.0.17
  flutter_blue_plus: ^1.15.5
  permission_handler: ^11.0.0
  crypto: ^3.0.3
  provider: ^6.0.5
  pointycastle: ^3.7.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/img/default.jpg
